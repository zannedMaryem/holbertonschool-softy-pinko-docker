
services:
  back-end:
    build: ./back-end
    ports:
      - "5252:5252"

  front-end:
    build: ./front-end
    ports:
      - "9000:9000"

  proxy:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./proxy.conf:/etc/nginx/conf.d/default.conf
